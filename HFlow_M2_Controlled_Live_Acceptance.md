# HFlow：受控真实验收与第一个 DSH 小变更

**报告基线：** `2603a1e`。实施者报告 CLI/Fake M2、候选引用、安全清理已经接通，回归为 103 passed / 1 skipped。

**证据边界：** 本文件作者没有读取该本地提交源码，也没有复跑测试；本文件限定下一项任务，不是代码审计结论。

**当前新增 live 授权：0。** 本文件、建议预算、示例授权文本和现有凭据引用本身都不是用户授权。下文真实执行仅在用户明确批准后进行。这里的额度指 HFlow/探针额外派发的顶层任务，不等于当前开发会话的费用，也不等于底层模型请求数。

## 1. 结束基础设施扩展，保留已经完成的成果

将 `2603a1e` 作为 CLI 可操作的 M2 离线基线。核对实际 HEAD，记录必要差异，不 reset、amend、stash 或丢弃用户改动。

本轮不继续扩展 porcelain 解析器、clean、Job、线程模型、传输适配、模型路由或协议取消。不要求支持子模块、冲突工作区、任意 ignored 文件清理、其他 Harness 或团队。既有危险情况明确拒绝是可接受行为。

M0 账本与历史强停试验保持不变。先前 INCONCLUSIVE 不回填成通过。报告中的历史 `DISPATCHED_PENDING` 不等于当前仍有活进程：按所属 invocation 与终态证据关联；无法确认则阻塞，不删历史、不新建台账。

成功标准从“更多内部测试”改为：**真实 DSH 经现有 CLI/控制器/Driver 产生一个可用候选，固定验收通过，独立审查完成，证据与候选一致。**

## 2. 先确认有合法的手动执行路径，而不只是拒绝路径

读取现有 CLI、授权检查、预算入口及实际 TaskSpec。`live_authorization_missing` 的负向测试证明未授权时拒绝，不单独证明已授权时整条路径可用。

若现有实现已有受控手动执行入口，直接复用。若只是无条件拒绝，允许补最小接线：将明确的用户授权、目标 TaskSpec/项目、执行绑定与额度传入现有控制器。不得绕过 CLI 直接调用 Driver 来冒充 CLI 验收。

只做以下必要约束，不建立权限服务、审批数据库、签名平台或新配置体系：

- 授权来自用户明确确认；模型不得把自己写的说明复制成 `authorization-note` 并冒充授权。
- 绑定本次操作类型、目标项目、固定基线、有效 TaskSpec 和允许角色；不能复用到另一任务、另一项目或另一执行模式。
- 复用已有预算/attempt 记录；重新启动进程、改 run ID 或重新提交同一授权都不能恢复额度。
- 未授权、额度不足、能力条件不满足时，在读取凭据、创建写入工作区和真实启动前拒绝；不降级成 Fake。
- 受控手动执行与 `unattended_execution` 分开。保持无人值守关闭，不新增万能 `--force` 或将全局开关改成 true。
- CLI 的实际参数以现有 help/代码为准，给出真正可复制的命令；不得在报告中发明尚未实现的参数。

已有入口满足则不改代码。必要接线用替身验证授权正/负路径，并复用真实 acpx→mock 检查；不发真实 DSH prompt，不读取凭据。仅当启动代码/绑定相关变化时重跑受影响的零模型检查，不重跑完整 M0。

## 3. 建议用户一次确认两项独立授权，合计最多 3 次

这是建议，不是已获准的额度。两项独立记录，不能挪用，也不能靠新实验 ID 追加。

| 授权 | 上限 | 目的 | 进入条件 |
|---|---:|---|---|
| A：真实强停补测 | 1 个顶层任务 | 复用已有 helper 与探针，补真实本地停止证据 | 当前执行组合的必要零模型检查成立 |
| B：M2 真实小变更 | 最多 2 个顶层任务 | 实现者 1 次；新会话 Reviewer 1 次 | A 对本次实际执行组合 PASS，且业务任务/审查前置成立 |

A 失败或 INCONCLUSIVE：不启动 B。B 实现失败、验证失败、审查拒绝、超时或结果未知：保存候选与证据，停止；不自动修复、换模型、再审或重派。未使用的后续额度不得拿来重试前一个步骤。

实施者和 Reviewer 都由选定 DSH 执行，角色定义仍保持 Harness-neutral。Codex、额外 Planner、研究 Agent、原生子代理的新增额度均为 0。

预算是顶层提交上限，不是付费 API 次数/Token 的精确上限。ACP 一次 prompt 可以包含多次模型与工具交互；上下文 used/size 不是账单。[S1] 沿用已有单调时钟截止、输出上限和停止机制，不为本轮新建计费平台。

### 供用户单独确认的文字（不能视作本文件已授权）

> 我批准基于 HFlow `2603a1e` 及为本次执行所需、已记录并通过零模型检查的最小接线，进行一次受控验收。授权 A：最多 1 次真实强停补测，沿用已有探针与 helper；只有 A 对实际执行组合 PASS 后，才可使用授权 B：针对既有 M2 示例业务缺陷，在独立无敏感数据的测试仓库中执行最多 2 次真实顶层任务，分别为 DSH 实现者 1 次和新会话 DSH Reviewer 1 次。两项分别记账，不挪用；失败、超时、结果未知或审查拒绝即停止，不自动重试、修复或增加调用。允许为本次隔离目录使用既有、已获准的凭据引用，不扩大读取范围、不打印或落盘凭据。不调用 Codex、额外 Planner 或子代理，不修改全局环境，不自动 merge/push/release/clean，不启用无人值守。我理解 Reviewer 当前仅采用明确披露的可信本地约束与事后审计，不是强只读沙箱；本地强停也不证明云端计费已经停止。

如果用户只批准其中一项，严格只执行那一项。没有新的明确授权，执行到第 2 节和第 5 节的离线准备即可，交付实际命令与明确缺口，不自行启动 A/B。

## 4. A：复用现有强停试验，不再开新开发分支

复用 `live_forced_stop_probe.py`、固定 helper、既有 Deadline/Job 归属检查和同一 Driver 的停止入口。不得重写一个旁路探针。

试验前确认实际 HFlow SHA、acpx 版本、DSH 版本、profile/启动绑定与零模型证据相符。能力记录按相关组合复用；单纯 clean 文案变更不触发全部传输重测。

沿用原强停指令的 READY、具体 Job 归属、运行窗口和退出确认条件。当前协议取消仍为 `unsupported_for_selected_exec`；目标是 `forced_local_stop_live`，不要求把协议取消变绿。

A 的 PASS 必须来自真实 DSH 启动的 helper，经实际控制器/Driver 停止，并有受管进程退出证据。helper 自行结束、外层直接启动 helper 或手工清理成功都不能替代它。

若无法得到有效停止窗口，记录 INCONCLUSIVE 并结束本验收包；不消耗 B 的名额补测。A 通过只更新本机相关组合的本地停止能力，不开启无人值守，也不宣称强沙箱或云端终止。[S3]

A 之后不要修改 Driver/停止机制再继续声称同一证据成立；必须修改相关代码时，本验收包暂停，不自动扩大实测预算。

## 5. B 的任务准备：复用 M2 的同一业务问题，不把答案复制过去

先读取现有 `examples/m2_cli_demo.py`、任务样例、项目配置和测试。沿用示例里已经固定的业务缺陷及其验收要求，在独立、无敏感数据、无远程生产连接的临时 Git 项目重建原始失败基线。

不要直接让真实 Agent 修改 HFlow 自身、旧 workflow、门店服务、支付/账户/生产数据库。此轮是合成测试项目中的真实代码交付，不包装成门店功能已经上线。

准备过程仅用程序，允许发生在没有 live 授权时：

1. 建立正确的原始 base，记录 commit 与固定项目/TaskSpec；确认原验收存在与目标缺陷一致的失败，而不是导入/依赖/测试发现失败。
2. 原检查应稳定且无需新依赖、网络或生产服务。记录检查命令和检查文件摘要，派发后不能放宽、删改或改成跳过。
3. 允许写入范围优先限定为示例对应的一个源码文件；确有既定需求再允许额外回归测试，不让实现者修改验收规则、框架代码、配置或预算。
4. 真实执行绝不使用 `--fake-write-plan`，不先调用 Fake 把候选修好，不给实现者预制的最终补丁或完整替换内容。可以提供需求、已知失败及相关源码/测试。
5. 派发包只包含当前任务所需的规则与文件引用，不把历次 HFlow 回报和全部方案加入上下文。已有复用决策直接引用，不重新搜索生态或启动研究 Agent。

不得向模型派发任务后才发现没有足够审查预算。沿用已有准入检查，开始时确保业务上限能够覆盖本轮要求的两次独立 invocation；实际失败时不为了用完预算继续派发。

## 6. B 的执行：一次实现、一次验证、一轮审查

### 6.1 实现与候选

通过真实 `hflow run` 入口选择 `acpx-dsh` 与 `workspace.mode=worktree`，进入现有控制器和授权预算路径。传输绑定与模式不从模型返回内容读取。

实现者只处理这个小缺陷。模型自行运行调试命令可以作为实现过程的活动记录，但不能替代控制器的正式验证，也不意味着要随后额外派发一个“测试 Agent”。

实际进程结束并确认结果完整后，沿用现有冻结逻辑得到真实 candidate commit/tree 与专用 ref。候选必须与 base 存在符合任务的有效差异。禁止继续由开发会话手工修补候选后把它算作本轮 DSH 实现成功。

工作区隔离并不是权限沙箱；linked worktree 会共享 Git 对象/部分元数据。保持用户 HEAD/index/工作文件/stash 不被覆盖，允许已记录的 HFlow 管理元数据变化，不声称 `.git` 零写入。[S2]

### 6.2 程序正式验证

使用派发前固定的验收命令和检查输入，对同一冻结候选运行。记录命令、工作目录、实际收集/执行结果、退出码、证据位置与候选标识。

所有测试未发现、全部意外跳过、检查文件被删改、候选/验证输入漂移、越界写入等不能作为通过。没有这些迹象就沿用现有规则，不新增通用测试报告平台。

程序检查失败：不启动 Reviewer、不修复重试；保留实际差异和错误。验证通过才进入审查。

### 6.3 独立真实 Reviewer

使用同一 DSH Harness，但创建不同 invocation/session。提供需求、基线/候选 diff、必要源码和正式验证证据，不提供实现者整段对话或自我辩护。

Reviewer 给出现有契约所需的结论与具体问题，不修改候选、不重新安排实现、不重复整套已有效的正式验证。重复或补充模型审查不在本轮额度内。

Reviewer 的限制采用已有能力并如实标注 `prompt_only/audit_only`；前述推荐授权只针对可信、一次性、低风险项目接受该限制，不把工作区/Job 或模型声明当作强只读证明。TaskSpec 若要求强只读而本机不能满足，就在派发前阻塞，不能临时降级该 TaskSpec。审查前后候选及固定检查输入必须一致，发现写入则审查无效并停止。

若现有生产 Reviewer 结果接线尚未实现，只补复用现有 Driver/结果契约所必需的薄接线并先离线验证。不能用 Fake verdict、实施者自评或手写 accepted 替代真实 Reviewer。不要用额外模型调用修 JSON 格式来消耗第三个业务名额；无法按现有规则确定性处理时，保存结果并阻塞。

### 6.4 接受与保留

只有有效候选、固定程序检查通过、有效独立审查接受且没有取消/结果未知/输入漂移时，由控制器生成 `ACCEPTED / LOCAL_CANDIDATE`。

`rc=0`、`end_turn` 或模型声称完成都不是业务接受的充分条件；这些是运行层事实，ACP 的 end_turn 仅描述 Agent 结束响应。[S1]

本轮交付到本地引用和 receipt 即结束。不自动合并、push、发布、删除候选引用或调用 `clean --apply`。需要时只使用现有只读预览；无需再次用真实任务验证清理器。

## 7. 结果报告：一张表，不再一套材料

复用已有 operations/ADR/运行结果，报告以下实际事实，不要求新增字段名或第二套状态机：

| 项目 | 报告内容 |
|---|---|
| 实际版本 | HFlow SHA、必要执行绑定与目标项目 base |
| 授权 A | 准入/发送/阻塞计数、PASS/FAIL/INCONCLUSIVE、引用已有证据 |
| 授权 B | 实现者与 Reviewer 各自 invocation/session、实际派发数、停止原因 |
| 候选交付 | base/candidate/tree/ref、是否有有效 diff、receipt/日志位置 |
| 验证与审查 | 固定检查实际结果、独立审查结论、候选及检查输入是否保持一致 |
| 成本与限制 | 可得用量、耗时、人工介入、unknown；不虚报账单或节省比例 |
| 权限/放行 | 本地停止能力、实际审查隔离等级、unattended 仍 disabled |

分别记录人工干预与额外模型派发，防止将运行器外手工修复隐藏在成功结果里。一次样例成功只能证明该任务链路可用，不是相对直接 DSH 更省钱的统计结论。

A PASS + B 被控制器接受：称为 **M2 首个真实 DSH 小变更闭环通过**。下一步选择受控日常项目任务或 M3 已规划的必要能力，不开启 Team/第二 Harness，也不重开传输工程。

A 或 B 失败：保留实际成果、授权消耗和证据。一个有具体证据的局部缺陷可以离线修复，但不能在修复后自动重跑 live，更不能递归修整套工作流。

没有新增代码时，可以没有新的代码提交。若只更新脱敏证据，提交信息应准确描述结果；未通过不命名为 passed。已有历史不改写。

## 8. 已核对的边界来源

- 本轮用户提供的 `2603a1e` 实施回报：全部本机实现、测试与调用事实的来源；本文件未独立复核。
- 既有《HFlow_M2_CLI_Cleanup_Next_Instruction.md》第 3、5、6 节：CLI 同一控制器、未授权拒绝、真实交付尚未完成。
- 既有《HFlow_Live_Forced_Stop_Instruction.md》：复用原强停试验的观察窗口、结果分类和单次预算；不重写探针。
- [S1] ACP Prompt Turn：一次顶层任务、多次模型/工具交互、用量与结束语义。https://agentclientprotocol.com/protocol/v1/prompt-turn
- [S2] Git worktree：linked worktree 与共享对象/元数据。https://git-scm.com/docs/git-worktree
- [S3] Microsoft Job Objects：本地关联进程管理与安全边界。https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects

**最终目标：不再把真实调用用来调试解释器、CLI 或清理器，而是在已有边界内完成一个真实的小改动，并保留一个确实可以使用的候选。**
