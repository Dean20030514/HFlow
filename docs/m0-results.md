# M0 transport probe results

Generated by `tools/m0_probe/render_results.py` from recorded artifacts only - no live task
was re-run to produce this table. Rows are grouped by what actually produced the evidence:

* `documented` - what the upstream/installed package claims.
* `local_observation` - what a read-only local check showed on this machine.
* `mock` - `acpx -> mock ACP agent` (a mock replaces DSH, so this is **not** DSH evidence).
* `live` - `acpx -> real DSH ACP`; the experimental budget was 2 top-level submissions, and
  the recorded sequence used 3 attempts - one of which the budget counter correctly refused.
  That is a top-level task count, not an API-request count and not a billing figure.

An untested capability stays `not tested`; nothing is inferred from a neighbouring row.

| capability | documented | local_observation | mock | live | conclusion |
|---|---|---|---|---|---|
| launcher boot of acp profile in probe-private home | `dsh --profile acp` starts the shipped stdio server | profiles created: ['acp'], dump-config exit=0 | not applicable | see live rows below | the launcher initializes the shipped template into a probe-private DSH_HOME; no prompt sent, so no inference |
| acpx custom agent via structured argv on Windows | custom agent registry; raw command strings rejected on win32 | `--agent "<command string>"` rejected on win32 with an explicit argv-array error; `agents.<name>.argv` config path also probed by the acpx CLI | argv agent launched and handshaked | argv agent launched the real DSH (see live rows) | the structured argv boundary works on Windows; the one-string form does not |
| initialize handshake | ACP initialize negotiates version and capabilities | ACP probe client sent protocolVersion=1 | exit=0 | see live rows below | verified against both mock and real DSH |
| session create + one prompt turn | session/new then session/prompt returns a stopReason | not checked | exit=0 | see live rows below | mock and real DSH both produce a stopReason |
| streamed session/update delivery | agent_message_chunk / agent_thought_chunk updates | not checked | exit=0 | see live rows below | update kinds observed live: see the prompt-turn row |
| normal termination and exit code | exit code 0 when the task completed, 1 when it aborted or errored | not checked | exit=0 on success; exit=1 on error | see live rows below | exit status distinguishes success from failure in both layers |
| deterministic protocol error handling | JSON-RPC error surface | not checked | unknown-method exit=1; garbage-line exit=0; bad-init exit=0 | not tested | an explicit JSON-RPC error fails the run; a malformed initialize *response* was accepted and the turn continued, so this client does not validate that handshake shape |
| server death without a reply | client must report failure rather than hang | not checked | exit=1 | not tested | client reports a failure instead of blocking |
| client timeout | --timeout bounds the wait | not checked | exit=3 (distinct exit code, acpxCode=TIMEOUT) | not tested | bounded wait with an explicit timeout error |
| cancellation of an in-flight turn | cooperative session/cancel; acpx cancel / prompt-owned cancellation | not checked | not observed: sending CTRL_BREAK to the client killed it (exit 0xC000013A) before any session/cancel reached the agent | not tested | NOT VERIFIED - the cooperative cancel path was never exercised; a hard client stop is not evidence of cancellation capability |
| real DSH ACP: server start, initialize, session open/close (no prompt) | `dsh --profile acp` starts the shipped stdio ACP server | agentInfo={'name': 'deepseek-harness-acp', 'version': '0.0.1'}; sessionCapabilities={"close": {}, "list": {}, "resume": {}}; session=0facd589-4996-4ce9-bf8e-7fb1aaa80550; model option present=True | not applicable | outcome=completed, server_rc=None | handshake and session lifecycle work; zero prompts, so zero inference |
| real DSH ACP: one prompt turn end to end | prompt -> semantic updates -> stop reason | one top-level submission per row | mock agent only; not evidence about DSH | task1-no-credentials: rc=1 nonce_echoed=False stop=[] updates=[] \| task2-fresh-session: rc=0 nonce_echoed=True stop=['end_turn'] updates=['agent_message_chunk', 'usage_update', 'tool_call', 'tool_call_update', 'agent_thought_chunk'] | round trip verified with controlled credential injection; without credentials the turn fails with an explicit no-API-key error |
| launch, observe, collect, force-stop, reconcile through the thin driver | neutral Driver contract | Job Object boundary verified standalone (2 processes -> 0) | 24 contract tests pass offline | one live task sent, client never started (wrong interpreter) - INCONCLUSIVE | forced stop proven offline; live forced stop still `not_tested`; cooperative cancel unsupported on this launch path |
