# --- runtime data (never committed) ---
.run/
*.sqlite
*.sqlite-wal
*.sqlite-shm
artifacts/

# --- M0 probe scratch: local acpx dependency tree, isolated homes, wire logs ---
# Reproducible via tools/m0_probe/run_probe.py; never committed (it is a dependency
# directory plus probe-private home/session/log data, not source).
.probe/

# --- test / tool temp ---
.pytest_cache/
.tmp/
tmp/

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
